{
  "name": "nurseroster-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npx serve public -l ${FRONTEND_PORT:-5173}",
    "preview": "npx serve public -l ${FRONTEND_PORT:-5173}",
    "build": "echo \"Static frontend served from public; no build step.\""
  }
}
